#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int r, c, result;
    cin >> r >> c;
    result = (r * c) - (ceil(r/3.0) * ceil(c/3.0));
    cout << result << endl;
    return 0;
}