#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int k = 1;
    while (n > 0) {
        n -= k;
        if (n >= 0) {
            k++;
        }
    }

    std::cout << k-1;

    return 0;
}